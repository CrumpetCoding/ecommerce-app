type PaginationButtonProps = {
    currentPage: number,
    pageNumber: number,
    totalPages: number,
}

export default function PaginationButton({ currentPage, pageNumber, totalPages }: PaginationButtonProps) {
    return (
    )
}

